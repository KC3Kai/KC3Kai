(function(){
	"use strict";
	
	/*KC3Network.addListener("api_get_member/questlist", function(eventName, data){
		
	});*/
	
	KC3Network.addListener("Quests", function(eventName, data){
		
	});
	
	KC3Network.addListener("BattleResult", function(eventName, data){
		
	});
	
	/*	
		function(event, data){
		if(isRunning || (["GameStart","HomeScreen","CatBomb"].indexOf(event)+1)){
			if(typeof NatsuiroListeners[event] != "undefined"){
				NatsuiroListeners[event](data);
			} else {
				console.warn("No event found for keyword",event);
			}
		}
	});
	*/
	
})();